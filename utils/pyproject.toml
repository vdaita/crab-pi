[project]
name = "utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=4.8.2",
    "huggingface-hub>=1.7.1",
    "numpy>=2.2.6",
    "safetensors>=0.7.0",
    "torch>=2.10.0",
    "transformers>=5.3.0",
]
